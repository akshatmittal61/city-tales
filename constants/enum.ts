const USER_ROLES = {
	ADMIN: "admin",
	USER: "user",
	GUEST: "guest",
};

const OTP_TYPES = {
	REGISTER: "register",
	RESET_PASSWORD: "reset_password",
};

const RESPONSE_MESSAGES = {
	SUCCESS: "Success",
	FAILED: "Failed",
};

export { USER_ROLES, OTP_TYPES, RESPONSE_MESSAGES };
